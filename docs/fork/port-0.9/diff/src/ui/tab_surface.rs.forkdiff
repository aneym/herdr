--- /tmp/base.tmp	2026-09-16 17:53:33
+++ docs/fork/port-0.9/orig/src/ui/tab_surface.rs	2026-09-16 17:53:33
@@ -105,7 +105,7 @@
     terminal_runtimes: &TerminalRuntimeRegistry,
     surface: TabSurfaceView<'_>,
 ) -> Option<CursorState> {
-    if app.mode != Mode::Terminal {
+    if app.mode() != Mode::Terminal {
         return None;
     }
 
@@ -189,7 +189,7 @@
         app.workspaces = vec![workspace];
         app.active = Some(0);
         app.selected = 0;
-        app.mode = Mode::Terminal;
+        app.replace_mode(Mode::Terminal);
 
         let full_area = Rect::new(0, 0, 106, 20);
         crate::ui::compute_view(&mut app, full_area);
@@ -284,7 +284,7 @@
         app.workspaces = vec![workspace];
         app.active = Some(0);
         app.selected = 0;
-        app.mode = Mode::Terminal;
+        app.replace_mode(Mode::Terminal);
         app
     }
 
@@ -304,14 +304,14 @@
         assert_eq!(frame.hyperlinks, vec![uri.to_owned()]);
         assert_eq!(
             frame_digest(&frame),
-            "a7c21fa42305a41231c7ae254f264f6ef923f46301d8fc4cd35ab6dfdd651b6b"
+            "636a11f12d3e46815ec3c12b66d20b7eb2203aa2b85a030137087a0b588c1132"
         );
     }
 
     #[tokio::test]
     async fn mobile_full_app_semantic_frame_is_characterized() {
         let mut app = full_app_characterization_state("https://example.com/mobile");
-        app.mode = Mode::Navigate;
+        app.replace_mode(Mode::Navigate);
         let frame = full_app_frame(&mut app, Rect::new(0, 0, 44, 20));
 
         assert_eq!((frame.width, frame.height), (44, 20));
@@ -321,7 +321,7 @@
         assert_eq!(frame.cursor, None);
         assert_eq!(
             frame_digest(&frame),
-            "295608a66067f1e1f066c0adb3cf427e8a2d68bba8f68949fb72d464dcd8baab"
+            "31aa420f0338b3361aa9a26ccea6b2a4ca28567e846a5c9f1349b65c70bdfd7e"
         );
     }
 }
